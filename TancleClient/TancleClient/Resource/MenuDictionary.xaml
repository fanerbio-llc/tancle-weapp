<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:TancleClient.ViewModel"
                    xmlns:v="clr-namespace:TancleClient.View">
    <DataTemplate DataType="{x:Type vm:SicknessManagementViewModel}">
        <v:SicknessManagement></v:SicknessManagement>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:HabitManagementViewModel}">
        <v:HabitManagement></v:HabitManagement>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AreaManagementViewModel}">
        <v:AreaManagement></v:AreaManagement>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AdviceManagementViewModel}">
        <v:AdviceManagement></v:AdviceManagement>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:PaginatorViewModel}">
        <v:Paginator></v:Paginator>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:SearcherViewModel}">
        <v:Searcher></v:Searcher>
    </DataTemplate>
</ResourceDictionary>